### 2. Comma Operators (Operadores de Coma)

El operador de coma permite evaluar múltiples expresiones y devuelve el valor de la última expresión evaluada.

**Ejemplo:**

```js
let x = (1, 2, 3);
console.log(x); // Resultado: 3
```

